{
  "name": "@openauthfederated/sdks",
  "private": true,
  "version": "0.1.0",
  "description": "OpenAuthFederated official SDKs — @auth/react (client) and @auth/backend (server).",
  "packageManager": "pnpm@11.0.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r exec rm -rf dist"
  }
}
